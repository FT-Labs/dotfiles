#!/bin/bash

b=$(basename $PWD)
mkdir tmp && cp * tmp || cd tmp
makepkg --sign
yes | rm -r $HOME/.local/src/phyOS-repo/x86_64/*$b*
yes | mv *.zst *.sig "$HOME/.local/src/phyOS-repo/x86_64/"
cd ..
yes | rm -r tmp
